name: 📦 Build PWA Android APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: ☕ Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🧱 Install Bubblewrap CLI
        run: npm install -g @bubblewrap/cli

      - name: 🧩 Create Bubblewrap project manually (skip init)
        run: |
          mkdir -p twa
          cd twa
          cat > twa-manifest.json <<'EOF'
          {
            "packageId": "com.powertech.generate_links",
            "name": "OTT 链接生成器",
            "startUrl": "https://powertech0417.github.io/generate_links/",
            "display": "standalone",
            "themeColor": "#000000",
            "backgroundColor": "#0a1a3a",
            "iconUrl": "https://powertech0417.github.io/generate_links/icon.png",
            "generatorApp": "bubblewrap-cli",
            "fullScopeUrl": "https://powertech0417.github.io/generate_links/"
          }
          EOF
          cat > config.json <<'EOF'
          {
            "manifestUrl": "https://powertech0417.github.io/generate_links/manifest.json",
            "host": "powertech0417.github.io",
            "name": "PowerTech OTT Generator",
            "launcherName": "OTT Generator",
            "display": "standalone",
            "themeColor": "#000000",
            "backgroundColor": "#0a1a3a",
            "navigationColor": "#000000",
            "orientation": "portrait",
            "fullScopeUrl": "https://powertech0417.github.io/generate_links/",
            "versionCode": 1,
            "versionName": "1.0",
            "signingKey": {
              "path": "./android.keystore",
              "alias": "my-key-alias",
              "password": "android",
              "keystorePassword": "android"
            }
          }
          EOF

      - name: 🏗️ Build APK
        run: |
          cd twa
          npx bubblewrap build --nonInteractive --skipPwaValidation

      - name: 📦 Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: PowerTech-OTT-Generator
          path: twa/output/app-release.apk
